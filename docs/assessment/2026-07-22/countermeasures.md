# Countermeasures Plan — 2026-07-22

Companion to [findings.md](./findings.md). Each finding maps to a countermeasure with a status:

- ✅ **Fixed in this pass** (code + regression test, TDD)
- 📋 **Deferred** — planned, with rationale and acceptance criteria. Not silently dropped.

Per repo policy: tests first for every fix; no speculative hardening; dead code deleted, not preserved "just in case".

## Phase 1 — Correctness & security fixes (this pass)

| #   | Finding                              | Countermeasure                                                                                                                                                                                                                                                                                                                                                                                                                      | Tests                                                                                                                           |
| --- | ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| F1  | S1 RLS integration tests red/skipped | Add operator-context seed helper (`withOperatorDb` via the Better Auth superuser pool) in `evals/harness/eval-context.ts`; migrate all user-seeding in the 5 affected specs to it                                                                                                                                                                                                                                                   | The 5 specs themselves go green against local Postgres                                                                          |
| F2  | S3 tier-1 non-atomic capture         | Wrap `capture_session` + `thought` inserts in one DB transaction. On AGE `upsertThoughtNode` failure: log loudly, **still schedule the enrich worker**, return success (tier-1 contract = row persisted). Tier 2 always re-ensures the anchor: remove `skipThoughtNodeUpsert: true` from `enrich-queued-thought.ts` and delete the now-unused option from `enrich.ts` / `entity-graph-sync.ts`                                      | New: AGE failure → capture succeeds + worker scheduled + enrich re-ensures anchor; transaction rollback → no orphan session row |
| F3  | S5 neighbor nondeterminism           | `ORDER BY weight DESC, neighbor_id ASC` on the `thought_neighbor` query in `retrieve-evidence.ts`                                                                                                                                                                                                                                                                                                                                   | New: deterministic, weight-ordered selection test                                                                               |
| F4  | S6 redundancyPenalty                 | Rewrite: two-pass, order-independent; count distinct communities per row; penalize only the candidate's **own** over-represented communities (>2 rows → +0.02 each), +0.05 if community_bundle-sourced, cap 0.15                                                                                                                                                                                                                    | Updated `retrieve-evidence.spec.ts` fixtures (no duplicate-id exploit); determinism test                                        |
| F5  | S6 score-max invariant               | Keep behavior (clamping is correct for MCP thresholds); name `TEMPORAL_BOOST = 0.18`, document that raw merge score max = 1.0 + 0.18 and `normalizeRetrievalScore` clamps by design                                                                                                                                                                                                                                                 | New: clamp invariant test                                                                                                       |
| F6  | S9 fragile 402 classification        | New typed `LlmHttpError` (`status` property) thrown by `llm-client`; `retry.ts` classifies via `instanceof`/`name` + `status === 402` instead of message substring                                                                                                                                                                                                                                                                  | New: typed-402 fatal test; non-402 LLM errors still retried                                                                     |
| F7  | S7 dead code                         | Delete: `fusion.ts`(+spec), deprecated RRF fns in `rrf-scoring.ts`, `temporal-conflicts.ts`(+spec) + its 2 call sites + `findTemporalSchedulingConflictsInGraph` in `age.ts` + graph-contract types, `detectContradictions` stub + call site, `inferQueryTimeRange` stub, `searchGlobal`(+spec block), `hasCommunitySummaries` if prod-unused, `persistCapturedThought`(+spec block), `void m` loop, `bundleRank` dead select/patch | Suite stays green after deletion; `check`/lint clean                                                                            |
| F8  | S2 RLS gaps                          | Add ENABLE/FORCE + tenant policies for `thought_entity`, `thought_neighbor`, `entity_top_thoughts`, `community_bundle`, `agent_project_binding`, `feedback`, `eval_qa` — **gated on the access-pattern audit** (any GUC-less access fixed first); document `consolidation_run` and `user` as intentional exceptions in `enable_rls.sql` header                                                                                      | Apply to local DB; full unit suite + integration specs green                                                                    |
| F9  | S4 cypher escaping untested          | Hostile-input unit tests for `toCypherLiteral`/`renderCypherQuery` (quotes, backslashes, dollar-quote collision, arrays, non-finite numbers, objects) — fix escaper if a case escapes                                                                                                                                                                                                                                               | New `age-cypher.spec.ts` cases                                                                                                  |
| F10 | S8 doc drift                         | Update `docs/repo-map/index.md` (weighted merge), `retrieval.md` (RERANK_POOL=15, shouldSkipRerank, temporal live AGE reads, no searchGlobal), `ingestion.md` (tier-1 atomicity + AGE best-effort anchor), CI workflow comment (89% floor), this assessment cross-linked from repo-map                                                                                                                                              | Docs match code after Ph1 lands                                                                                                 |

## Phase 2 — Deferred (planned, not done in this pass)

| #   | Finding                                | Countermeasure                                                                                                                                                               | Rationale for deferral / acceptance                                                                                 |
| --- | -------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| D1  | S1 integration specs skip in CI        | Add a CI job that builds the `db` image (pgvector+AGE), runs `migrate.mjs` + `apply-rls.mjs`, then `test:unit` with `DATABASE_URL`                                           | Needs operator decision on image build/publish time in CI. Acceptance: integration specs run (not skip) on every PR |
| D2  | S10 coverage enforcement               | Raise floors toward 95% critical; drop `continue-on-error` once green; un-exclude `graph/**`/`consolidation/**` after their specs land                                       | Ratcheting policy — raising floors without the new specs would block all merges                                     |
| D3  | S10 untested targets                   | Specs for `tenant-session.ts`, `trace-cost.ts`, `gateway-providers.ts`, `graph-sync-worker.ts` (real behavior), `api/thoughts/[thoughtId]/+server.ts`                        | Pure test-writing; independent stream                                                                               |
| D4  | S9 in-process recovery                 | Heartbeat/lease on enrich worker rows so multi-instance deploys recover crashed workers                                                                                      | Only matters for multi-instance deploys; single-instance today                                                      |
| D5  | S10 E2E gaps                           | Playwright: capture→enrich→search round trip; offline queue flush; MCP tool journey                                                                                          | Operator-owned E2E; needs local stack conventions                                                                   |
| D6  | S10 string-heuristics static guard     | Add `assert:no-string-heuristics` script + spec scanning production code for forbidden patterns (mirror `assert:oss-secrets`)                                                | Pattern list needs care to avoid false positives (lexical indexing is allowed)                                      |
| D7  | S11 double graph representation        | Document the decision in `docs/repo-map/retrieval.md` (materialization = hot path; AGE = viz/temporal/consolidation; `entity_resolution_log` = source of truth for rebuilds) | Documentation decision, folded into F10 where relevant                                                              |
| D8  | S11 pgvector ops                       | Set `hnsw.ef_search` at query time if recall issues appear; schedule periodic `ANALYZE`                                                                                      | No measured recall problem today; evals (`/eval` UI) are the trigger                                                |
| D9  | S9 client/server retry mismatch        | Align client queue to 1+3 total attempts (or document why 3 total is intentional)                                                                                            | Needs UX decision on offline-retry duration                                                                         |
| D10 | S11 no live-path dedup / token budgets | Reintroduce near-dup detection as a deliberate feature (the dead `persistCapturedThought` heuristic is deleted in F7); add per-call token budgets                            | Feature work, needs product decision                                                                                |

## Verification for this pass

1. `npm run test:unit` — green (expect 0 failures incl. the previously red 7 with `DATABASE_URL` set).
2. `npm run check` + `npm run lint` — clean.
3. `npm run db:rls` against local Postgres — policies apply; integration specs re-run green after applying.
4. Docs updated to match shipped behavior (repo-map maintenance rule: smallest affected layer).

---

## Results — 2026-07-23 (this pass landed)

**Unit suite: 433 files / 2697 tests green, 0 failures** (baseline was 2665 passed + 7 failed). `npm run db:rls` applied the 6 new policies to the local DB; integration specs verified live against them. Note: `npm run check` reports ~299 pre-existing errors on main (verified at HEAD — unchanged by this pass).

| #   | Status | What shipped                                                                                                                                                                                                                                                                                                                                  |
| --- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| F1  | ✅     | `withOperatorDb` in `evals/harness/eval-context.ts`; 5 specs migrated (user seed/teardown now operator-context; ontology seeded for the thought-category FK; duplicate per-test user inserts hoisted to `beforeAll`; stale `'thought'` category fixture → `'observation'`). All 11 integration tests green — tenant isolation verified again. |
| F2  | ✅     | `queueCapture`: session+thought now one transaction; AGE anchor upsert is best-effort with loud `console.error` + worker still scheduled (no more 500 → duplicate-submit path); `skipThoughtNodeUpsert` option deleted — tier-2 always re-ensures the anchor (`entity-graph-sync.ts`). Regression tests: tx usage, AGE-down capture success.  |
| F3  | ✅     | Neighbor expansion: `ORDER BY weight DESC, neighbor_id ASC` (`retrieve-evidence.ts`) + orderBy assertion test.                                                                                                                                                                                                                                |
| F4  | ✅     | `redundancyPenalty` rewritten: pool-wide counts, per-row community dedupe, membership-aware, order-independent. Tests pin uncrowded/crowded scores (0.034 vs 0.014) and the duplicate-id regression.                                                                                                                                          |
| F5  | ✅     | `TEMPORAL_BOOST = 0.18` named + documented (raw max 1.18, clamp by design); clamp tests in `rrf-scoring.spec.ts`; temporal-boost score pinned at 0.264.                                                                                                                                                                                       |

| F6 | ✅ | `src/lib/server/llm/errors.ts` (`LlmHttpError` with `status`) thrown from both `llm-client` throw sites; `retry.ts` classifies fatal via type+status (no message sniffing). Tests: 402 fatal after 1 attempt, 500 retried to budget, `InsufficientCreditsError` fatal. |
| F7 | ✅ | Deleted: `fusion.ts`(+spec), `temporal-conflicts.ts`(+spec) + both call sites + `findTemporalSchedulingConflictsInGraph` (age.ts) + `TemporalSchedulingConflictGraphHit` (graph-contract), deprecated RRF helpers (`RRF_K`, `maxFusedRrfScore`, `normalizeFusedRrfScore`), `detectContradictions` + `mergeConflictPairs` + `hydrateConflictThoughts` + `prioritizeConflictThoughts` (compose-answer), `inferQueryTimeRange`, `searchGlobal` + `hasCommunitySummaries` (global.ts rewritten), `persistCapturedThought` (+spec block), `void m` loop, dead `bundleRank` plumbing. Suite green throughout. |
| F8 | ✅ | 6 new tenant policies live in `enable_rls.sql` (`thought_entity`, `thought_neighbor`, `entity_top_thoughts`, `community_bundle`, `agent_project_binding`, `feedback`) after the access audit found zero GUC-less accesses; exceptions (`user`, `consolidation_run`, `eval_qa`) documented in the file header. **Bonus fix:** `scheduleIncrementalConsolidation` now self-wraps `withDbUser` (audit caveat: detached task could outlive the caller's tenant session) + new spec. Applied locally; full suite green under stricter RLS. |
| F9 | ✅ | 10 hostile-input tests for `toCypherLiteral` / `renderCypherQuery` / `wrapAgeCypherDollarQuote` (quote+backslash breakout, nested arrays, param-like values, delimiter escalation, non-finite numbers, objects). Escaper held — no code change needed; param-position caveat documented. |
| F10 | ✅ | `repo-map/index.md` (weighted merge + assessment link), `retrieval.md` (RERANK_POOL=15, `shouldSkipRerank`, temporal live-AGE exception, `searchGlobal` deletion), `ingestion.md` (tier-1 atomicity + best-effort anchor), CI workflow comment (89% floor). |

**Not done (needs the operator/owners):** D1–D10 remain open as planned above. Also observed but out of scope: (a) `npm run check` has ~299 pre-existing errors on main (incl. `queue-capture.spec.ts` `source: 'web'` vs `captureSourceEnum`, `IngestPhase` union drift, `LLM_REQUEST_TIMEOUT_MS` env-mock drift, `age.ts` GraphVizEdge kind drift) — the CI `check` gate is presumably red on main already and deserves its own pass; (b) parallel uncommitted work in this tree (mcp/tools, brain.schema +75 lines, journal, i18n messages, timeline) was left untouched; the full unit suite is green with it included.
