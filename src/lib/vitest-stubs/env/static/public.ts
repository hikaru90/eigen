/**
 * Browser-mode stub for `$env/static/public`.
 *
 * Analytics init no-ops without a key, so empty strings are the safe,
 * leak-free default for component specs.
 */
export const PUBLIC_POSTHOG_HOST = ''
export const PUBLIC_POSTHOG_KEY = ''
