/**
 * Browser-mode stub for `$app/environment`.
 *
 * Specs run in a real browser, so `browser` is true — but the lean browser
 * config has no SvelteKit plugin, so the module must exist on disk.
 */
export const browser = true
