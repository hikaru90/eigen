/**
 * Browser-mode stub for `$app/navigation`.
 *
 * Component specs never complete a real form action, so `invalidateAll` only
 * needs to exist and resolve.
 */
export async function invalidateAll(): Promise<void> {}
