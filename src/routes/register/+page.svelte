<script lang="ts">
  // Unreachable: register/+page.server.ts always redirects to /signup (see page.server.spec.ts).
  // Kept as a stub so the route stays registered; the signup page owns the account form.
</script>
